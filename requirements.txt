# Redrob AI Ranker — dependencies
# Python 3.11+

# Embedding model (offline, CPU)
sentence-transformers==3.4.1
torch==2.6.0          # CPU-only; install with: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers==4.51.3

# Fast vector operations (numpy-based cosine sim over 100K × 384)
numpy==2.2.4
faiss-cpu==1.10.0     # optional — only needed if we switch to ANN retrieval

# Data handling
tqdm==4.67.1          # progress bars during embedding precomputation

# Validation / submission
pandas==2.2.3         # CSV output only; not used for core logic
